<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.jared.thefouthmoosade.ui.MainActivity">

    <ImageView
        android:layout_width="1sp"
        android:layout_height="1sp"
        app:srcCompat="@drawable/page0"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:id="@+id/imageView"
        android:contentDescription="APP"/>

    <Button
        android:text="Start The Adventure!"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/startButton"
        android:backgroundTint="@android:color/background_light"/>

    <TextView
        android:text="The Fourth MOOsade"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/imageView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:id="@+id/titleTextView"
        android:textSize="65sp"
        android:textAlignment="center"
        android:textAllCaps="true"
        android:textStyle="normal|bold"/>

    <TextView
        android:text="By Jared Martin and Brayden Pusser"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/titleTextView"
        android:layout_centerHorizontal="true"
        android:id="@+id/subtitleTextView"
        android:textSize="20sp"
        android:textAlignment="center"
        android:textAllCaps="false"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:layout_above="@+id/startButton"
        android:layout_centerHorizontal="true"
        android:id="@+id/nameEditText"
        android:hint="Enter Your Name to Start!"
        android:maxWidth="30dp"/>
</RelativeLayout>
